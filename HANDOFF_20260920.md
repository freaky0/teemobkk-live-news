# 이어서 작업하기 — 2026-09-20

새 세션(또는 다른 기기)에서 이 프로젝트를 그대로 이어받기 위한 문서. 위에서부터 읽으면 된다.
(이번 갱신: 같은 날 두 번째 세션. 분류 세분화·다중 라벨과 배포 게이트가 들어갔다.)

## 30초 요약

- 무엇: 방콕 교민용 실시간 뉴스 대시보드 + 랜딩 페이지, 내 PC 없이 24시간 도는 공개 사이트
- 이 저장소: `C:\AI\Work_Folders\News_Macros\live_news_dashboard` (branch `main`, 원격 `freaky0/teemobkk-live-news`)
- 운영: VPS `72.62.64.195` (`/opt/teemo-live-news`) → https://teemobkk.io (Caddy TLS + systemd)
- 지금 상태: 6개 경로 200 · DB **13,214행** · 최근 커밋 `01c3a68` · 검사 **14/14**(로컬+운영)

## 새 세션이 이어받는 순서

1. 이 문서를 끝까지 읽는다.
2. 스킬을 로드한다: `skill_view('live-news-monitoring')` — `references/` 11개.
3. 상태를 한 번에 확인한다. 배포 게이트가 같은 검사를 돌리므로 여기서 실패하면 배포도 막힌다.
   ```
   python tests/run.py            # 로컬 + 운영
   python tests/run.py --local    # 로컬만 (로컬 대시보드가 떠 있어야 함)
   python tests/run.py --live     # 운영만
   ```
4. 이전 대화의 세부가 필요하면 검색한다:
   `session_search(session_id='20260914_081851_7ddce9', query='키워드')`

## 떠 있는 것 / 다시 띄우는 법

| 무엇 | 어떻게 |
|---|---|
| 로컬 대시보드 | `cmd /c start_dashboard_bg.bat` → http://127.0.0.1:8765 (이전 리스너를 스스로 정리하고, 포트가 열린 뒤에만 성공을 반환) |
| 운영 배포 | `bash deploy/vps_update.sh` (로컬에서 실행. 검사 → 소유권 → pull → **마이그레이션** → 페이지 재생성 → 재시작 → 6개 경로 검증) |
| VPS 상태 | `ssh root@72.62.64.195` → `systemctl status teemo-live-news` |
| VPS 로그 | `journalctl -u teemo-live-news -n 50` |
| DB 확인 | `ssh root@72.62.64.195 "python3 -c \"import sqlite3;print(sqlite3.connect('file:/opt/teemo-live-news/news.db?mode=ro',uri=True).execute('SELECT COUNT(*) FROM articles').fetchone())\""` |

**배포 순서를 바꾸지 않는다**: 검사 → 소유권 복구 → `git pull` → **DB 마이그레이션** → **페이지 재생성** → 재시작 → 검증.
마이그레이션이 재생성보다 먼저인 이유: 페이지 생성기가 DB 행을 읽는다. 순서가 뒤집히면 첫 화면 시드가
빈 채로 배포된다(실측: 시드 카드 0개 — t30의 `시드 카드 실려 있음`이 잡아냈다).

## 이번 세션에서 끝낸 것 (전부 배포·검증 완료)

- **검사 자동 게이트** — `deploy/vps_update.sh`가 호스트를 건드리기 전에 `python tests/run.py --local`을
  돌리고 실패하면 멈춘다. `SKIP_TESTS=1`로 우회하면 "검증 없이 배포함"을 출력에 남긴다.
  jsdom을 `tests/node_modules`에 설치하고 잠금 파일을 커밋해 검사 폴더가 자립했다(예전에는 72시간 뒤
  삭제되는 임시 폴더에서 읽었다). 로컬 대시보드가 없으면 통과가 아니라 **거부**한다.
  배포 후 검증은 6개 주소 전부 200을 요구한다(이전에는 대시보드만 봤다).
- **VPS 잔존 정리** — DB 교체가 남긴 12개 파일을 `/opt/teemo-archive/db-swap-20260920/`으로 옮기고
  `MANIFEST.md`에 sha256·정합성·행수를 기록했다. 셋은 **바이트 단위로 같은 파일**(`34a73408…`, 795행)이고
  진짜 손상본은 `news.db.malformed-005641`(`745cd7f8…`) 하나였다. 삭제는 하지 않았다.
  `.gitignore`에 `news.db.bak-*`·`news.db.replaced-*`·`news.db.malformed-*`·`news.db.retag-*.bak`를 넣어
  다음 교체·재계산 때 미추적으로 뜨지 않게 했다.
- **분류 세분화 + 다중 라벨** — `유동성·금리` → `금리`, `미국 정책·트럼프` → `미국 정책` + `트럼프`.
  한 기사가 **여러 축**을 갖고, 카드는 그만큼 알약을 보여주며, 알약 하나를 누르면 그 축을 가진 기사를
  모두 찾는다(합집합). 저장은 `category`(첫 값) + `categories`(쉼표 목록) 두 열이다.
  사전은 `category_rules.py`로 나갔다(코어에는 import 한 줄만).
- **첫 화면 시드 복구** — 위 배포 순서 문제. `seed_from_db`가 목록 열이 없는 DB에서도 단일 값으로
  시드하게 만들고, 배포가 재생성 전에 마이그레이션을 돌린다.

## 지금 상태 (실측, 이번 세션)

- 분류 사전: 영문 15개(금리, 미국 정책, 트럼프, ETF·수급, 규제·정책, 지정학, 파생상품·청산, 온체인·기관,
  스테이블코인, X 발언, 주식·원자재, 채굴, 거시경제, 시장·가격, 이더리움·알트) + `일반`. 태국 7개는 그대로.
- 라벨 분포(24시간 창, 13,183행): 1개 10,627 · 2개 1,939 · 3개 이상 617 → 카드 19%가 알약 두 개, 4.7%가 세 개.
- 첫 분류 분포: 금리 529 · 미국 정책 212 · 트럼프 495 (옛 두 이름 1,236행이 셋으로 갈렸고, 다른 분류 수는 불변).
- 운영 API 확인: `category=트럼프` 68건 · `미국 정책` 25건 · `금리` 15건.

## 핵심 규칙 (어기면 사고가 난다)

- **코어(`live_news_dashboard.py`)에 로직을 쌓지 않는다.** 수집기·사전·표시 로직은 별도 파일에서.
  사전을 바꾸는 일은 `category_rules.py`에서 한다.
- **분류 이름을 나눌 때는 어휘를 그대로 옮긴다.** 첫 분류가 중요도 보너스를 결정하므로 어휘가 늘면
  별점이 움직인다. 실측: `rate cut`·`rate hike`·`fed funds`·`역레포`·`기준금리`를 넣으면 111행이 ★4→★3.
  검증은 `tools/verify_taxonomy.py --old <이전 코어> --` 가 어휘 **집합 동등**(추가 0·누락 0)을 본다.
- **공식을 바꾸면 이동량을 먼저 잰다.** `tools/baseline_scores.py --old <이전 코어>` — 저장값과 이전 코어
  계산의 차이(기존 드리프트)와 이번 변경이 만든 이동을 갈라서 보고한다. 현재 기존 드리프트 85행은
  손대지 않고 기록만 했다.
- **목록을 읽는 함수는 두 모양을 모두 받아야 한다.** DB 계층은 리스트를 주고, 테이블에서 직접 읽은 행은
  문자열이다. 문자열만 받게 만들면 목록이 자기 repr이 되어 화면에 `['일반']`이 찍힌다(실측).
  이름이 바뀐 값은 `retire()`가 표의 낱말과 대조해 정리한다(하드코딩 없음).
- **한국어·태국어를 파일에 쓴 뒤에는 코드포인트로 되읽어 검증한다.** 글자 누락·치환이 반복적으로
  일어난다. 손상 복구는 ASCII 전용 스크립트 + `\uXXXX` 조립으로 한다.
- **`.sh` 는 LF.** `.gitattributes` 에 `*.sh text eol=lf` 가 있다. 개행은 `grep -c $'\r'`로 보지 말고
  바이트로 센다 — MSYS grep은 이 파일에서 0바이트를 94로 보고했다(오독).
- **MSYS curl은 `-o /dev/null`에서 200에도 exit 23을 낸다.** 상태를 exit code로 판정하지 말고
  `curl -s ... >/dev/null`처럼 셸 리다이렉션을 쓴다(게이트가 항상 막히는 원인이었다).
- **VPS 파일 소유권** — root 로 tar 를 풀면 서비스가 못 읽는다. 배포 첫 단계가 `chown teemo:teemo`.
- **자격증명은 채팅에 붙여넣지 않는다.** 토큰은 사용자가 직접 등록한다.
- **되돌릴 수 있게** — 배포 전 git tag + VPS 파일 백업. 재계산 도구는 `--apply` 전에 리포트를 내고,
  적용할 때 `VACUUM INTO`로 백업을 만든다(실행 중인 WAL DB는 파일 복사로는 깨진 사본이 된다).

## 남은 일

1. **분류 어휘 확장(측정 대기)** — `금리`에 `rate cut`·`rate hike`·`fed funds`·`역레포`·`기준금리`를
   넣는 안. 측정치: 111행이 ★4→★3(거시경제·규제·정책에서 금리로 첫 분류가 옮겨가며 보너스를 잃음).
   별점을 감수할지, 보너스 집합을 손볼지 정해야 한다. 사용자 판단 대기.
2. **Cloudflare 워커 미정지** — 꺼진 워크플로를 5분마다 두드려 로그만 쌓인다. 이 PC에 wrangler
   자격증명이 없어 Cloudflare 로그인 없이는 못 한다.
3. **지표 이미지 435KB 핫링크** — 트레이딩뷰 S3 에서 직접 받는다. 자체 호스팅 권장.
4. **블로그 입구** — 화면에 "서브스택" 문자열을 쓰지 않는 것이 Astra 검사 조건이라 블로그 링크가 없다.
5. **시장 관점·지표 목록이 수동 상수(`EDITORIAL_POSTS`)** — 글이 늘면 손으로 고쳐야 한다.
6. **P6 AI 채점 키 등록 대기** — 무료 티어 키 등록 전에는 시작할 수 없다.
7. **`vision_analyze` 제공자 고장** — 화면을 눈으로 볼 수 없다. 기하 측정 · 계산된 스타일 ·
   픽셀 해시로 대체 검증한다.
8. **기존 점수 드리프트 85행** — 저장된 `priority`가 새로 계산한 값과 다르다(이번 변경과 무관, 오래된
   드리프트). 그대로 두었다. 정리하려면 별도 판단이 필요하다.
9. **Decrypt 중복은 정책상 유지** — 링크가 키라서 같은 기사가 RSS와 Bluesky로 들어오면 별개 행이다.
   48시간 기준 같은 제목·다른 링크 그룹 181개, 그중 SNS 미러 관련 27개. 제목을 키로 넣으면 다른 매체의
   동명 기사가 합쳐지므로 지금은 건드리지 않는다.
10. **태국 정적 페이지 회귀 2건은 재현되지 않는다** — `probe_thai.py --live` 18/18, `t31` 6/6 통과.
11. **`docs/` 는 이제 리다이렉트 사본** — `docs/*.html`은 teemobkk.io 로 넘기는 얇은 페이지이고,
    `docs/*.json`은 로컬 운영자 페이지의 첫 화면 시드를 만드는 자료다. 이 JSON의 분류 이름도
    검사(`test_categories.py`)가 실제 표의 이름인지 확인한다.

## 검사 도구

```
python tests/run.py [--local|--live]          # jsdom 페이지 검사 + 단위 계약 (로컬 11 · 운영 3)
python -m unittest test_landing               # 랜딩 계약 (루트)
python tests/test_categories.py               # 분류 다중 라벨 계약 (15개, 서버 불필요)
python test_landing_browser.py [--live]       # 랜딩 실브라우저 (18/18)
python tools/probe_keywords.py <url>          # 키워드 줄 기하·색·조합
python tools/probe_label_pixels.py <url>      # 라벨 겹침 픽셀 증명
python tools/probe_homepage.py --live         # 루트 랜딩 (20/20)
python tools/probe_thai.py --live             # 태국 랜딩 (18/18)
python tools/verify_taxonomy.py --old <이전 코어>   # 어휘 집합 동등성 + 옛 이름 retire 확인
python tools/baseline_scores.py --old <이전 코어>   # 기존 드리프트와 이번 변경의 이동 분리
python tools/retag_categories.py [--apply] [--db PATH]   # 저장 행 재계산(기본은 리포트)
python tools/probe_category_split.py          # 분할 후보별 영향 측정
python tools/probe_category_terms.py          # 어느 낱말이 끌어왔는지 귀속
python tools/probe_category_overlap.py        # 축 겹침·다중 라벨 분포
python tools/shot_keywords.py <url>           # 캡처 (Temp\shots)
```

- 이전 코어 사본을 뜨는 법: `git show HEAD:live_news_dashboard.py > /tmp/core_before.py`
  (분류 변경을 커밋하기 **전에** 떠야 한다. 커밋 뒤에는 그 커밋을 되짚어 꺼낸다.)
- 실브라우저 검사는 `HERMES_BROWSER_TOOLS="C:/Users/freak/AppData/Local/hermes/cache/scratch/teemo-browser-tools"`
  환경변수가 필요하다.
- **jsdom 은 `var()` 를 해석하지 못한다** — 색·배경 검사는 실브라우저 검사로 한다.

## VPS 헤르메스 에이전트 현황 (2026-09-20 확인)

도커로 돌고 있다: 컨테이너 `hermes-agent-hjzo-hermes-agent-1`, 홈 `/opt/data`
(호스트 `/docker/hermes-agent-hjzo/data`), `hermes dashboard` 4860 포트 + `hermes gateway`.

| 있는 것 | 없는 것 |
|---|---|
| node v26.5.1 · python 3.13.5 · git 2.47.3 | 호스트 파일시스템 접근 → `/opt/teemo-live-news` 못 봄 |
| 자기 홈(`/opt/data`) 영속 | docker 소켓 · systemctl → 서비스 재시작 못 함 |
| 네트워크(teemobkk.io 조회 가능) | `gh` · GitHub 자격증명 → push 못 함 |

**그래서 UI 작업을 이어받기엔 부적합하다.** 이어받을 곳은 이 PC 의 새 세션이다.
**대신 잘 맞는 일**: 24시간 감시 — 30분마다 `https://teemobkk.io/api/news?limit=1` 을 받아 최신 발행
시각이 35분을 넘으면 알리는 크론(내 PC 가 꺼져 있어도 돈다).
