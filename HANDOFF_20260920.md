# 이어서 작업하기 — 2026-09-20

새 세션(또는 다른 기기)에서 이 프로젝트를 그대로 이어받기 위한 문서. 위에서부터 읽으면 된다.

## 30초 요약

- 무엇: 방콕 교민용 실시간 뉴스 대시보드 + 랜딩 페이지, 내 PC 없이 24시간 도는 공개 사이트
- 이 저장소: `C:\AI\Work_Folders\News_Macros\live_news_dashboard` (branch `main`, 원격 `freaky0/teemobkk-live-news`)
- 운영: VPS `72.62.64.195` (`/opt/teemo-live-news`) → https://teemobkk.io (Caddy TLS + systemd)
- 지금 상태: 6개 경로 200 · DB 13,081행 · 최근 커밋 `9a395d9`

## 새 세션이 이어받는 순서

1. 이 문서를 끝까지 읽는다.
2. 스킬을 로드한다: `skill_view('live-news-monitoring')` — v1.21.0, `references/` 11개
   (첫 페이지·자체 서버 호스팅·언어와 시드·RSS·갱신 주기 등)
3. 검사를 한 번 돌려 지금 상태를 확인한다:
   ```
   python tests/run.py            # 로컬 + 운영
   python tests/run.py --local    # 로컬만 (로컬 대시보드가 떠 있어야 함)
   python tests/run.py --live     # 운영만
   ```
4. 이전 대화의 세부가 필요하면 검색한다. 이 세션에는 4,914개 메시지가 남아 있다:
   ```
   session_search(session_id='20260914_081851_7ddce9', query='키워드 멀티셀렉')
   ```

## 떠 있는 것 / 다시 띄우는 법

| 무엇 | 어떻게 |
|---|---|
| 로컬 대시보드 | `live_news_dashboard/start_dashboard_bg.bat` → http://127.0.0.1:8765 |
| 운영 배포 | `bash deploy/vps_update.sh` (로컬에서 실행, ssh 키 인증) |
| VPS 상태 | `ssh root@72.62.64.195` → `systemctl status teemo-live-news` |
| VPS 로그 | `journalctl -u teemo-live-news -n 50` |
| DB 확인 | `ssh root@72.62.64.195 "python3 -c \"import sqlite3;print(sqlite3.connect('file:/opt/teemo-live-news/news.db?mode=ro',uri=True).execute('SELECT COUNT(*) FROM articles').fetchone())\""` |

**배포 스크립트의 순서를 바꾸지 않는다**: 소유권 복구 → `git pull` → **페이지 재생성** → 재시작 → 경로 검증.
서버가 요청마다 디스크의 `index.html` 을 읽고, 저장소에는 운영용 페이지가 커밋돼 있다.
재생성을 건너뛰면 운영 페이지가 그대로 공개된다.

## 최근에 한 일 (전부 배포·검증 완료)

- **키워드 멀티셀렉** — 낱말 여러 개를 동시에. API는 `q` 하나만 받으므로 낱말마다 따로 조회해
  링크로 병합한다(창 전체를 훑는다). 같은 낱말을 다시 누르면 풀린다.
- **분류(탭) 멀티셀렉 + 키워드 조합** — `V.cats` 가 목록이 되고, 두 축을 교차한 조합마다 요청한다
  (분류 둘 × 낱말 둘 = 네 요청). `전체` 는 분류·키워드·검색어를 한 번에 푼다.
- **키워드 줄 겹침 수정** — 라벨이 스크롤 상자 안에 sticky 였고 높이가 23px(버튼 32px)이라
  버튼이 위·아래로 삐져나와 글자와 겹쳐 보였다. 라벨을 스크롤 상자의 **형제**로 분리했다.
  라벨 영역 픽셀이 스크롤 위치와 무관하게 동일한 것으로 증명한다(`tools/probe_label_pixels.py`).
- **선택 표시 수정** — `.trend .tbtn{background:none}` 이 같은 특이도로 뒤에 있어 선택 채움을
  투명으로 덮고 있었다. 선택 규칙을 한 단계 구체적으로(`.trend .tbtn.on`) 쓰고 채움 + 체크를
  넣었으며, 호버는 `@media (hover:hover)` 안으로 넣었다(터치에서 `:hover` 가 눌러붙는 문제).
- **검사 하네스를 저장소로 옮김** — `tests/` (임시 폴더는 72시간 뒤 지워져 사라질 위험이 있었다).

## 핵심 규칙 (어기면 사고가 난다)

- **코어(`live_news_dashboard.py`)를 직접 패치하지 않는다.** 수집기·표시 로직은 별도 파일에서.
  신규 수집기를 붙일 때 `import` + `fetch_into` 두 줄만 예외.
- **한국어·태국어를 파일에 쓴 뒤에는 코드포인트로 되읽어 검증한다.** 글자 누락·치환이 반복적으로
  일어난다. 손상 복구는 ASCII 전용 스크립트 + `\uXXXX` 조립으로 한다. `write_file` 은 조용히
  실패한 사례가 있으므로 `io.open` 또는 `patch` 를 쓴다.
- **`.sh` 는 LF.** `.gitattributes` 에 `*.sh text eol=lf` 가 있다. CRLF면 호스트에서 즉시 실패한다.
- **VPS 파일 소유권** — root 로 tar 를 풀면 서비스가 못 읽는다. 배포 스크립트 첫 단계가 `chown teemo:teemo`.
- **자격증명은 채팅에 붙여넣지 않는다.** 값은 어시스턴트가 보지 않는다. 토큰은 사용자가 직접 등록한다.
- **되돌릴 수 있게** — 배포 전 git tag + VPS 파일 백업. 기존 파일을 고치지 않고 새 파일로 만들되,
  직전 글의 완성본은 갱신한다.

## 남은 일

1. **화면에 보이는 분류 이름** — 지금은 저장값(`일반`, `유동성·금리`)을 그대로 보여준다. 짧은 화면용
   라벨로 바꿀지는 사용자 판단 대기.
2. **Cloudflare 워커 미정지** — GitHub Actions 수집은 `disabled_manually` 인데 워커가 아직 `*/5` 로
   꺼진 워크플로를 두드려 로그에 오류만 쌓인다. 크론 비우기 또는 워커 삭제 필요.
3. **지표 이미지 435KB 핫링크** — 트레이딩뷰 S3 에서 직접 받는다. 자체 호스팅 권장.
4. **블로그 입구** — 화면에 "서브스택" 문자열을 쓰지 않는 것이 Astra 검사 조건이라 블로그 링크가 없다.
5. **시장 관점·지표 목록이 수동 상수(`EDITORIAL_POSTS`)** — 글이 늘면 손으로 고쳐야 한다.
6. **검사가 배포에 물려 있지 않다** — 지금은 수동 실행(`python tests/run.py`).
7. **VPS 에 손상 DB 백업 3개와 `news.db.replaced-*` 잔존** — 미추적 상태.
8. **공개 태국 정적 페이지 회귀 2건**, **Decrypt 기사 중복**(Bluesky·RSS 별개 행, 링크가 키).
9. **P6 AI 채점 키 등록 대기** — 무료 티어 키 등록 전에는 시작할 수 없다.
10. **`vision_analyze` 제공자 고장** — 화면을 눈으로 볼 수 없다. 기하 측정 · 계산된 스타일 ·
    픽셀 해시로 대체 검증한다.

## 검사 도구

```
python tests/run.py [--local|--live]          # jsdom 페이지 검사 + 랜딩 단위 계약
python -m unittest test_landing               # 랜딩 계약 (루트)
python test_landing_browser.py [--live]       # 랜딩 실브라우저 (18/18)
python tools/probe_keywords.py <url>          # 키워드 줄 기하·색·조합 (12/12)
python tools/probe_label_pixels.py <url>      # 라벨 겹침 픽셀 증명
python tools/probe_homepage.py --live         # 루트 랜딩 (20/20)
python tools/probe_thai.py --live             # 태국 랜딩 (18/18)
python tools/shot_keywords.py <url>           # 캡처 (Temp\shots)
```

- jsdom 은 `tests/node_modules` 를 먼저 보고, 없으면 예전 임시 하네스를 본다.
  자립시키려면 `cd tests && npm i jsdom`.
- 실브라우저 검사는 `HERMES_BROWSER_TOOLS="C:/Users/freak/AppData/Local/hermes/cache/scratch/teemo-browser-tools"`
  환경변수가 필요하다(playwright 위치).
- **jsdom 은 `var()` 를 해석하지 못한다** — 색·배경 검사는 실브라우저 검사로 한다.

## VPS 헤르메스 에이전트 현황 (2026-09-20 확인)

도커로 돌고 있다: 컨테이너 `hermes-agent-hjzo-hermes-agent-1`, 이미지
`ghcr.io/hostinger/hvps-hermes-agent:latest`, 홈 `/opt/data` (호스트 `/docker/hermes-agent-hjzo/data`),
`hermes dashboard` 4860 포트 + `hermes gateway`.

| 있는 것 | 없는 것 |
|---|---|
| node v26.5.1 · python 3.13.5 · git 2.47.3 | 호스트 파일시스템 접근 → `/opt/teemo-live-news` 못 봄 |
| 자기 홈(`/opt/data`) 영속 | docker 소켓 · systemctl → 서비스 재시작 못 함 |
| 네트워크(teemobkk.io 조회 가능) | `gh` · GitHub 자격증명 → push 못 함 |
| | 스킬 디렉터리 없음(프로필이 비어 있음) |

**그래서 UI 작업을 이어받기엔 부적합하다** — 저장소를 못 보고 서비스를 못 만지며, 실브라우저 검사
도구도 없다. 이어받을 곳은 이 PC 의 새 세션이다.

**대신 잘 맞는 일**: 24시간 감시. 예를 들어 30분마다 `https://teemobkk.io/api/news?limit=1` 을
받아 최신 발행 시각이 35분을 넘으면 알리는 크론 — 내 PC 가 꺼져 있어도 돌고, 저장소 접근이
필요 없다. (설정하려면 사용자 확인 후 진행)
