# 경제 뉴스 중심 홈페이지

## 범위
- 원본: `landing.py`, 생성: `render_landing()` / `page_build.build_server()`.
- 홈페이지만 변경. 뉴스·태국 페이지, 수집기, DB, 서버 설정은 유지.
- 태국 소식은 보조 링크. 시장 관점은 플랫폼 중립 이름이며 기존 실제 글로 연결.
- `EDITORIAL_POSTS`는 수동 관리 목록이다. 자동으로 새 글이 추가되지는 않는다.
- 지표 이미지는 실제 TradingView 공개 이미지 외부 URL. 원본 비율 유지 및 확대 제공.
- Elliott Wave / Scout Suite는 공식 개별 페이지에서 무료 사용 가능한 protected script임을 확인. 소스 공개와 무료 사용은 구분한다.

## 뉴스 표시
- `/api/news?region=글로벌&hours=24&limit=100`에서 최근 6건 표시, 2분 갱신.
- 태국·URL뿐인 제목·정규화 제목 중복·잘못된 링크·24시간 밖의 기사 제외.
- 일반 분류는 경제 관련 키워드로 제한. 이는 기본 규칙 필터이지 운영자 선정/팩트체크/의미 기반 중복 제거가 아니다.
- 새 기사 원문은 번역하지 않는다. 원문 언어와 자동 수집임을 명시.
- 수집 시각이 없거나 요청에 실패했을 때 현재 시각을 수집 시각으로 대체하지 않는다.
- 갱신 실패는 현재 화면 목록 유지. 초기 실패·빈 목록·35분 이상 갱신 지연 안내 및 재시도 제공.

## 검증
- `python -m unittest test_landing -v`
- Playwright 사용: `HERMES_BROWSER_TOOLS=<별도 설치 경로> python test_landing_browser.py`
- 운영 확인: 위 명령에 `--live` 추가.
- 브라우저 시험은 별도 headless Chromium 프로필 사용. 사용자 브라우저 설정 변경 없음.
- 로컬 브라우저 18개, 운영 브라우저 12개 검사 통과. 320/390/768/1024/1440 폭 가로 넘침 없음.
- 홈페이지 내부·외부 연결 8개 HTTP 200. 로컬 렌더 결과와 운영 응답 일치(서버 public 플래그 삽입 제외).

## 배포와 복구
- 서버 `/opt/teemo-live-news/landing.py`와 `index.html`만 원자적 교체, 서비스 재시작 없음.
- 원본 백업: `/opt/teemo-home-backups/20260920-020405/`.
- 복구 시 백업의 두 파일을 동일 경로로 되돌리고 소유권 `teemo:teemo` 유지. DB와 수집기는 건드리지 않는다.

## 남은 일
- 자체 블로그와 외부 발행 플랫폼 선택.
- 관점 목록 자동 갱신 또는 편집 관리 화면.
- 전체 뉴스의 주제/자산별 분류와 의미 기반 중복 묶음은 별도 작업.
- 실사용 기반 성능/접근성 전체 감사와 클릭 분석은 미도입.
