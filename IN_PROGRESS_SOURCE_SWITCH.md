# 진행 중 — 소스 공개 스위치 (다른 세션이 작업 중)

**이 파일은 한 세션의 작업 표시다. 다른 세션은 아래 파일들을 건드리지 말 것.**

## 무엇을 만드나

운영자 페이지(`/admin`)에서 **소스별 on/off 스위치**를 두고, 꺼진 소스의 기사를 배포 페이지에서
즉시 빼는 기능. 사용자 결정에 따라:

- 화면은 **운영 서버의 `/admin`** (로컬 페이지가 아니다)
- 저장은 **서버 DB** — `settings` 표의 `hidden_sources` 키 (JSON 배열). 즉시 반영되며 git 을 타지 않는다
- 수집은 계속한다. 숨김은 **표시에만** 적용되고, 되돌리면 과거 기사까지 함께 돌아온다

## 손대는 파일

```
live_news_dashboard.py   setting_get/setting_set 옆에 hidden_sources()·set_source_hidden()
                         query_articles 의 where 에 소스 제외 (숨김 기사와 같은 규칙: 모든 읽기 경로)
                         /api/status 응답에 hidden_sources
                         POST /api/source (기존 인증 검사 뒤)
page_build.py            운영자 사이드바에 '소스 공개' 상자 + 스위치 (기존 .source 행 스타일)
tests/test_source_switch.py   단위 검사 (신규)
tests/t38.js                  스위치 UI 검사 (신규)
tests/run.py                  위 둘을 목록에 추가
```

## 왜 이렇게 하는가

`query_articles` 에서 거르면 배포 대시보드·랜딩·알약·건수가 **자동으로** 함께 빠진다. 페이지마다
따로 실어 나를 필요가 없고, 서버가 한 곳에서 판단하므로 화면마다 어긋날 일이 없다. 대신 숨긴 소스의
기사는 운영자 피드에서도 사라지므로, 되돌리는 길은 피드가 아니라 스위치 목록이다(숨긴 기사가
`hidden_links` 를 따로 읽는 것과 같은 이유).

## 폐기한 접근 (참고용)

브랜치 `source-visibility-wip` (커밋 `e0186aa`)에 **저장소 파일 + 로컬 도우미** 방식이 통째로 있다.
동작은 확인했지만(도우미가 파일을 쓰고 페이지를 재생성) 대상 화면이 로컬 페이지라 배포 `/admin`
에서는 쓸 수 없다. 위 결정으로 대체되므로 **머지하지 않는다.**

## 상태

- [x] 결정: `/admin` + 서버 DB
- [ ] 코어: 설정 키·필터·엔드포인트
- [ ] 페이지: 스위치 UI
- [ ] 검사: 단위 + UI
- [ ] 배포·운영 검증
