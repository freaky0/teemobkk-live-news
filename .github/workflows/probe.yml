name: probe

# Temporary: tells us whether a plain run (no concurrency group) gets a runner
# while the collect runs sit pending. Delete once the cause is known.
on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "runner ok $(date -u)"
