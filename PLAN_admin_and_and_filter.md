# 계획 — 교차(AND) 필터 · 배포본 관리자 페이지 · Teemo's Pick

사용자 요청 3건을 실행 단계까지 쪼갠 문서. **아직 구현 전**이다(결정 대기 항목은 §6).
각 단계는 커밋 1개이며, 검증 명령을 통과해야 다음 단계로 간다.

## 0. 요약

1. 키워드/분류 멀티셀렉을 **합집합(OR)에서 교집합(AND)으로** 바꾼다. 지금은 선택한 낱말이 들어 있는
   기사를 **모두** 뿌린다. 원하는 것은 "고른 조건을 **전부** 가진 기사"다.
2. **배포본에 관리자 페이지**를 만든다(삭제·수집 간격·운영 패널). 완성되면 로컬 페이지는 은퇴한다.
3. 기사별 **삭제 버튼**과 **Teemo's Pick** 표시. Pick은 편집 표시이지 사실 검증 표시가 아니다.

## 1. 교차 필터 (AND)

### 1-1. 왜 그냥 AND로 바꾸면 안 되는가 (실측)

지금 API의 `q`는 **부분 문자열** 검색이다. 그래서 조건이 늘수록 결과가 급격히 준다.

| 조건 (24시간 창 1,337행) | 개별 | 교집합 |
|---|---|---|
| 트럼프 + 관세 | 48 · 1 | **0** |
| 트럼프 + AI(단어) | 48 · 47 | 8 |
| 연준 + 국채금리 | 10 · 0 | **0** |
| 이란 + 유가 | 16 · 5 | **0** |
| 파이낸셜쥬스 + 트럼프 | 48 · 48 | 3 |
| 트럼프 + 미국 정책(분류) | 67 · 25 | 18 |
| 파이낸셜쥬스 + 트럼프 + 관세 | 48 · 48 · 1 | **0** |

| 같은 조건 (전체 보관 = 7일, 13,225행) | 교집합 |
|---|---|
| 트럼프 + 관세 | 23 |
| 트럼프 + AI | 36 |
| 연준 + 국채금리 | 11 |
| 이란 + 유가 | 46 |
| 파이낸셜쥬스 + 트럼프 | 90 |
| 트럼프 + 미국 정책(분류) | 126 |
| 파이낸셜쥬스 + 트럼프 + 관세 | **0** (여전히) |
| 연준 + 물가 + 비트코인 | 6 |
| 이란 + 이스라엘 + 유가 | 1 |

두 가지가 결론이다.

- **교차는 24시간 창에서 거의 항상 0건이다.** 창을 전체 보관으로 넓혀야 쓸 만해진다(1,337행 → 13,225행).
- **넓혀도 0건인 조합이 정상적으로 있다**(파이낸셜쥬스+트럼프+관세).

**확정: 창은 24시간 그대로 두고 자동으로 넓히지 않는다.** 대신 0건이면 조건별 건수와 가장 좁은
조건을 보여주고, **"더 긴 기간에서 찾기" 버튼**을 띄운다 — 누를 때만 전체 보관(현재 7일)으로 넓히고,
넓힌 사실과 유효 기간을 화면에 표시한다. 읽는 사람이 "지금 24시간에는 없다"와 "아예 없다"를
구분할 수 있어야 하고, 자동 확대는 목록에 며칠 전 기사를 조용히 섞어 넣는다.

### 1-2. 어휘 잡음도 함께 고친다 (실측)

| 낱말 | 부분일치 | 단어 경계 | 잡음 |
|---|---|---|---|
| AI | 3,720 | 599 | **6.2배** |
| tariff | 73 | 34 | 2.1배 |
| etf | 539 | 315 | 1.7배 |
| fed | 660 | 572 | 1.2배 |

`AI`를 고르면 "said·Thailand·chain"에 걸린 기사가 6배로 딸려 온다. 교차 필터에서는 이 잡음이
결과를 통째로 망가뜨리므로 **라틴 문자 낱말은 단어 경계로 매칭**한다(한국어·태국어는 지금처럼 부분일치
— 띄어쓰기가 없어 경계를 잡을 수 없다. 사전 쪽 `BOUNDARY_TERMS`와 같은 원칙).

구현: `sqlite3` 연결에 매칭 함수를 등록해 SQL 안에서 판정한다(`connection.create_function`).
파이썬에서 후처리하면 `LIMIT/OFFSET` 계산이 어긋나 페이지 수가 틀어진다.

### 1-3. 규칙

| 축 | 선택 2개 이상일 때 | 이유 |
|---|---|---|
| 키워드(낱말·구절) | **AND** (모두 포함) | 요청 사항 |
| 분류 알약 | **AND** (모두 해당) | 다중 라벨이 생겨 실제로 가능해졌다(트럼프+미국 정책 126건/7일) |
| 소스 알약 | **OR** (이 중 하나) | 한 기사에 발행처는 하나뿐이라 AND는 항상 0건 |
| 축 사이(소스 × 분류 × 키워드) | **AND** | "파이낸셜쥬스 + 트럼프" = 파이낸셜쥬스가 쓴 트럼프 기사 |

### 1-4. 화면

- 선택된 조건마다 **개별 건수**를 함께 표시: `파이낸셜쥬스 1,303 · 트럼프 462 · 관세 34 → 0건`.
- 0건일 때: **가장 좁은 조건**을 짚고(`관세 34건이 가장 좁습니다`), **"더 긴 기간에서 찾기"** 버튼을 준다.
  누르면 기간이 전체 보관으로 바뀌고 상단에 그 사실을 표시한다(예: `전체 기간(7일) · 조건 3개 모두`).
  이때 무엇을 풀면 몇 건인지도 함께 보여준다(`관세 풀면 90건`).
- 검색창에 친 낱말을 **`+ 조건 추가`로 칩으로 만들 수 있게** 한다. 추세 줄은 규칙(라틴 3자 이상)을
  그대로 두고, `AI`처럼 줄에 오르지 않는 낱말·구절은 여기로 넣는다. 그래야 추세 줄의 잡음 규칙과
  "내가 원하는 조건"이 서로를 방해하지 않는다.
- 요청은 **한 번**으로 줄어든다. 지금은 조합마다(분류 둘 × 낱말 둘 = 네 요청) 따로 받아 링크로
  합치는데, AND는 서버가 SQL로 판정하므로 그 병합 경로와 클라이언트 페이징 계산이 함께 사라진다.
- 조건이 늘면 카드의 어느 조각이 그 조건을 만족했는지 알 수 없으므로, 하이라이트는 지금처럼
  검색어 표시만 유지한다(칩에 "이 조건 때문에 왔다"는 표시는 붙이지 않는다).

### 1-5. 함정

- 클릭한 검색어가 원문에 없는 형태면 0건이 된다(추세 줄이 구절을 제목에 붙어 있을 때만 올리는
  이유와 같다). 구절 조건은 원문 문자열 그대로 보낸다.
- `AI` 같은 2글자 낱말은 지금 **추세 줄에 아예 오르지 않는다**(라틴 3자 이상 규칙). 낱말을 늘리려면
  예외 목록(AI·X·EU·US·UN·QT·QE)을 두거나 검색창에 직접 넣어야 한다 → §6-5.
- 정적 사본(`docs/`)은 받아 둔 300건 안에서만 AND가 걸린다. 배포본은 API 경로라 무관하지만
  문서에 남긴다.

## 2. 배포본 관리자 페이지

### 2-1. 지금 구조 (사실 확인)

- 서비스: `/usr/bin/python3 live_news_dashboard.py --public --interval 60` → `PUBLIC_MODE=True`.
  이 모드에서 쓰기 엔드포인트는 전부 막혀 있고 운영 패널도 응답에서 빠진다.
- Caddy: `/news/*`·`/thai/*`는 디스크 파일, 나머지는 전부 127.0.0.1:8765로 프록시.
  수집기는 `/`와 아이콘만 처리하므로 `/admin`은 지금 404.
- 즉 **관리자 페이지는 수집기가 직접 내보내야 한다.** `admin.html`을 Caddy가 서빙하는 디렉터리에
  두면 인증 없이 공개된다(절대 금지).

### 2-2. 인증 설계 (표준 라이브러리만)

- 비밀번호 파일: `/opt/teemo-live-news/.admin_password` (600, teemo 소유). 저장소에 넣지 않는다
  (`.gitignore`). 사용자가 서버에서 직접 만들도록 도구를 준다(`getpass`로 입력, 셸 기록에 안 남음).
- 로그인: `POST /api/login` → 비밀번호 대조 → `teemo_admin=<만료>.<HMAC>` 쿠키
  (`HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=30d`). 키는 비밀번호에서 유도하므로
  비밀번호를 바꾸면 기존 세션이 자동 만료된다.
- 판정은 **요청마다**: 쿠키가 유효하면 그 요청만 관리자로 취급(운영 패널 포함 페이지 + 쓰기 허용),
  아니면 지금과 동일한 공개 응답. `PUBLIC_MODE` 전역 플래그는 "관리자 세션 없음"의 기본값으로 남긴다.
- 실패 제한: IP당 15분에 5회 → 429. 시도·성공·실패·쓰기 동작을 로그 한 줄씩 남긴다(감사).
- 쓰기 엔드포인트는 JSON + `X-Requested-With` 헤더를 요구한다(CORS 밖 요청 차단 보강).
- 비밀번호는 채팅·코드·로그 어디에도 남기지 않는다. 잊으면 서버에서 파일을 다시 만든다.

### 2-3. 노출 방지 (검사로 고정)

- 익명 `GET /admin` → 로그인 폼만. 그 응답 본문에 운영 패널 문자열(`수집 상태`, `보관 13,2xx`, 설정 UI)이
  **0건**이어야 한다.
- 익명 `POST /api/article/hide`, `/api/article/pick`, `/api/settings` → **401**.
- 쿠키 위조(서명 불일치·만료) → **401**.
- 익명 `GET /api/news`·`/api/status` 응답에 `archive`·`sources`·`inserted` 필드가 **없어야** 한다(회귀).
- 이 5개를 `tests/`에 넣어 배포 게이트가 매번 돌린다.

### 2-4. 로컬 페이지 은퇴

`/admin/`이 위 검사를 통과하고 사용자가 확인한 뒤에 한다. 그때까지 로컬 페이지는 개발용으로 남기고,
은퇴 단계에서: 시작 배치의 안내를 "개발용"으로 바꾸고, 루트 `index.html`(운영자 판)을 만들지 않는
쪽으로 빌드를 정리한다(운영자 판 생성은 `admin.html` 하나로 합친다). 은퇴 후 개발 흐름은
로컬 API(`--public` 없이 실행) + `/api` 직접 호출로 유지한다.

## 3. 삭제와 Teemo's Pick

### 3-1. 삭제 = 숨김(되돌릴 수 있게)

- 물리 삭제하지 않는다. `hidden_links(link PRIMARY KEY, hidden_at, reason)` 테이블에 기록하고
  **조회 시점에 제외**한다(`query_articles`의 WHERE 한 줄).
- 물리 삭제를 안 하는 이유: (1) 잘못 누르면 되돌릴 수 없다, (2) 수집기는 `INSERT OR IGNORE`라
  다음 주기에 같은 기사가 그대로 다시 들어오므로 삭제만으로는 재등장을 막지 못한다,
  (3) 숨김은 감사 기록이 남는다.
- 그래서 **재등장 방어는 숨김 기록**이 담당한다. 같은 기사가 다른 링크(예: RSS와 Bluesky)로 들어오면
  별개 행이라 다시 보인다 — 이 한계는 문서에 밝히고, 필요하면 다음 단계에서 "제목이 같으면 함께 숨김"을
  측정 후 추가한다(48시간 기준 같은 제목 그룹 181개).
- 화면: 카드에 `삭제` 버튼(로그인 시에만), 필터 줄에 `숨긴 기사 N`(되돌리기 목록).

### 3-2. Teemo's Pick

- `picks(link PRIMARY KEY, picked_at, note)` — 수집 데이터와 분리해 둔다.
- 표시: 카드에 `Teemo's Pick` 배지(하트), 필터 줄에 `Teemo's Pick` 알약.
- **정렬은 그대로 최신순.** Pick을 위로 고정하지 않는다(사용자가 "업데이트가 멈춘 것처럼 보인다"고
  판단해 고정을 걷어낸 이력이 있다). 별점·`priority`도 건드리지 않는다.
- **문구는 사실 검증을 주장하지 않는다.** "Teemo가 고른 기사"라는 편집 표시이지 "검증됨"이 아니다.
  신뢰도 배지(공식/SNS)와 시각적으로 구분해 붙인다.
- 고른 기사는 보관 정리에서 **제외**한다(90일이 지나도 남긴다). Pick은 소수라 비용이 없고,
  나중에 "그때 내가 고른 것"이 사라지면 표시의 뜻이 없어진다.

## 4. 단계 (한 단계 = 커밋 하나)

| # | 단계 | 변경 | 검증 | 되돌리기 |
|---|---|---|---|---|
| S1 | 단어 경계 매칭 | `q`를 SQL 함수로 판정(라틴=단어, 한글·태국어=부분) | `AI` 부분일치 3,720→599, 기존 검색 결과 불변 확인 | 커밋 revert |
| S2 | AND 필터(서버) | `q`·`category` 반복 허용, 모두 AND, 소스는 OR | 조건별 개별 건수와 교집합이 실측표와 일치 | 커밋 revert |
| S3 | AND 필터(화면) | 조합 요청·병합 제거 → 한 요청, 조건별 건수·0건 안내·"더 긴 기간" 버튼, `+ 조건 추가` 칩 | jsdom: 조건 3개 선택 시 카드가 세 조건을 모두 포함, 0건 안내와 버튼 노출, 칩 추가/해제, 기간 확대 시 상단 표시 | 커밋 revert |
| S4 | 인증 뼈대 | 비밀번호 파일, `/api/login`·`/api/logout`, 서명 쿠키, 실패 제한 | 익명 401 / 정상 쿠키 200 / 위조 401 (5개 검사) | 파일 삭제 = 즉시 원복 |
| S5 | `/admin` 페이지 | 수집기가 `admin.html`을 인증 후 서빙, 요청별 판정 | 익명 본문에 운영 문자열 0건, 로그인 후 패널 표시 | 라우트 제거 |
| S6 | 숨김 | `hidden_links` + 조회 제외 + 되돌리기 UI | 숨긴 기사가 목록·API·개수에서 0건, 되돌리면 복귀, 수집 재삽입 후에도 0건 | 테이블 비우기 |
| S7 | Teemo's Pick | `picks` 테이블 + 배지 + 알약 + 보관 예외 | 배지 표시·알약 필터·정렬 불변(첫 항목 발행시각 동일)·익명도 표시 | 테이블 비우기 |
| S8 | 수집 간격 런타임 변경 | 설정 파일(`settings.json`) 영속, 재시작 후 유지 | 변경 → 로그 반영 → 재시작 → 값 유지 | 파일 삭제(기본값 복귀) |
| S9 | 로컬 페이지 은퇴 | 빌드·배치 문서 정리 | `/admin/` 검사 통과 + 로컬 페이지 없이 전체 검사 통과 | 문서 revert |

배포는 전 단계에서 기존 게이트(`deploy/vps_update.sh`)를 그대로 지난다. DB 스키마 변경(S6·S7)은
기존 `init_db` 마이그레이션 경로에 추가하고, 배포는 마이그레이션 → 재생성 → 재시작 순서를 유지한다.

측정 하네스: `python tools/probe_and_filter.py --hours 24 --hours2 2160` — 조건별 부분일치/단어일치
건수와 교집합을 실측한다. S1·S2의 before/after를 이 숫자로 비교한다(§1-1 표가 이 도구의 출력이다).

## 5. 보안 점검 (배포 전 체크리스트)

- 비밀번호 파일이 저장소·로그·응답에 없는가(`git grep`, `journalctl` 확인).
- 쿠키에 `HttpOnly`·`Secure`·`SameSite=Strict`가 붙었는가(응답 헤더로 확인).
- 익명으로 운영 데이터(`archive`·`sources`·`inserted`)가 새지 않는가.
- 쓰기 엔드포인트가 인증 없이 **401**인가(각각 실제 요청으로 확인).
- CSRF: `SameSite=Strict` + 커스텀 헤더 요구 + JSON 본문만.
- 실패 5회 후 429, 로그에 IP·시각 기록.
- Caddy 디렉터리에 `admin.html`을 두지 않았는가(디스크 서빙 경로 확인).

## 6. 확정된 결정 (2026-09-20)

| # | 항목 | 확정 내용 |
|---|---|---|
| 1 | 교차 적용 범위 | 키워드·분류는 **AND**, 소스는 **OR**, 축 사이는 **AND** |
| 2 | 0건일 때 | 기간은 **24시간 유지**. 조건별 건수 + 가장 좁은 조건 안내 + **"더 긴 기간에서 찾기" 버튼**(누를 때만 전체 보관) |
| 3 | 관리자 인증 | **앱 비밀번호 + 서명 쿠키**. 비밀번호는 서버 파일에 사용자가 직접 설정하며 어시스턴트는 값을 보지 않는다 |
| 4 | 삭제의 뜻 | **숨김 + 되돌리기**(`hidden_links`). 물리 삭제하지 않는다 |
| 5 | 2글자 낱말(AI) | 검색창 낱말을 **`+ 조건 추가` 칩**으로 만든다. 추세 줄 규칙(라틴 3자 이상)은 그대로 둔다 |
| 6 | Pick 표시 | **배지 + 알약**, 정렬은 최신순 그대로(고정하지 않음). `priority`·별점과 분리 |

## 7. 미리 밝히는 한계

- 같은 기사가 다른 링크로 들어오면(매체 RSS + 그 매체 소셜) 숨김·Pick이 그 사본에는 적용되지 않는다.
  링크가 키라는 결정의 대가다(같은 제목 그룹 181건/48시간).
- 교차 결과는 오래된 기사가 섞인다. 기간 표시를 눈에 띄게 두고, 발행 시각은 카드마다 이미 표시된다.
- 추세 줄의 2글자 낱말은 지금 규칙상 후보가 아니다(§6-6 결정 필요).
- 검색은 제목·요약만 본다(본문 수집 없음).
